{
  "name": "corgi-claude",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone agentic demo with OpenLedger, run end to end by an external Claude Code CLI.",
  "type": "module",
  "scripts": {
    "start": "tsx src/main.ts",
    "typecheck": "tsc --noEmit",
    "verify": "npm run typecheck && tsx src/verify.ts"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.5.0"
  }
}
